
// $body = $('body');

// $('#recent-bills').on('click', async function(event){

//     event.preventDefault();

//     $('#home-container').hide()


//     let response = await axios.get('/get-data');
//     const $newBillContainer = $('<div class="bill-container"></div>');
//     const $newBillName = $(`<h1>${response.data.title}</h1>`);
//     const $newBillDate = $(`<h2>${response.data.date_created}</h2>`);
//     const $newBillSponsor = $(`<h2>${response.data.sponsor}</h2>`);
//     const $newBillSummary = $(`<h2>${response.data.summary}</h2>`);


//     $body.append($newBillContainer);
//     $newBillContainer.append($newBillName, $newBillDate, $newBillSponsor, $newBillSummary);

// })

